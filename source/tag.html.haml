---
pageable: true
per_page: 12
---

%header.site-head{style: "background-image: url(/images/cover.jpg)"}
  .vertical
    .site-head-content.inner
      - if blog_logo?
        %a.blog-logo{href: '/'}
          = image_tag blog_logo, alt: 'Blog Logo'
      %h1.blog-title
        %a.blog-logo{href: '/'}
          = blog_name
      %h2.blog-description= tagname

%main.content{role: :main}
  - page_articles.each_with_index do |article, i|
    = partial :post_overview, :locals => {:article => article}

  - if paginate
    %nav.pagination{role: :pagination}
      - if prev_page
        = link_to '← Newer Posts', prev_page, class: 'newer-posts'
      %span.page-number Page #{page_number} of #{num_pages}
      - if next_page
        = link_to 'Older Posts →', next_page, class: 'older-posts'
